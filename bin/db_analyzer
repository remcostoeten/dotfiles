#!/usr/bin/env bash

# Bin wrapper for scripts/db_analyzer.py
# This allows calling the script as 'db_analyzer' from anywhere when bin/ is in PATH

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
DOTFILES_DIR="$(dirname "$SCRIPT_DIR")"
PYTHON_SCRIPT="$DOTFILES_DIR/scripts/db_analyzer.py"

# Check if Python script exists
if [[ ! -f "$PYTHON_SCRIPT" ]]; then
    echo "❌ Error: Cannot find db_analyzer.py script at $PYTHON_SCRIPT"
    exit 1
fi

# Execute the Python script with all arguments passed through
exec python3 "$PYTHON_SCRIPT" "$@"
