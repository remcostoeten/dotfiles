/**
 * App.tsx
 *
 * Minimal Vite app entry that just renders the mock auth demo.
 * Replace your existing src/App.tsx with this (or import MockAuthDemo
 * wherever you like).
 */

import { MockAuthDemo } from "./MockAuthDemo";

export default function App() {
  return <MockAuthDemo />;
}
